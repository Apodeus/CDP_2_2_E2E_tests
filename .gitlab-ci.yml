stages:
  - analyse

image:
  finanzcheck/docker-node-java:latest

sonar:
  stage: analyse
  before_script:
    - npm install sonar-scanner@3.1.0
  script:
    - ls
    - npm run sonar-scanner -Dsonar.projectKey=cdp:cdp -Dsonar.organization=apodeus-github -Dsonar.sources=src -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=4cd26046108dc8f90fdb2ea79bd015cdc1d98182
